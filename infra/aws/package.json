{
  "name": "@edutex/infra-aws",
  "version": "1.0.0-rc.6",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "presynth": "npm run build",
    "synth": "cdk synth",
    "test": "vitest run lib --coverage.enabled=false --passWithNoTests --exclude '**/dist/**'"
  },
  "devDependencies": {
    "aws-cdk": "2.1140.0",
    "aws-cdk-lib": "2.268.0",
    "constructs": "10.8.1",
    "source-map-support": "0.5.21",
    "vitest": "4.1.10"
  }
}
